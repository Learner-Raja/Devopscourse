pipeline {
    agent { label "Ansible-Master-Server" }
    stages {
        stage ("clone playbook") {
            steps {
                checkout scm
            }
        }
        stage ("Users") {
            steps {
                script {
                    sh "/var/lib/jenkins/workspace/Ansible-pipeline/httpd.yml"
                    sh "ansible-playbook httpd.yml"
                }
            }
        }
    }
    post {
        success {
            echo "The Build is sucess"
        }
        failure {
            echo "The Build is failed"
        }
    }
}


